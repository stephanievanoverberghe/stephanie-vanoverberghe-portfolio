// src/components/layout/header/header.styles.ts

export const ribbonStyle: React.CSSProperties = {
    background: 'linear-gradient(90deg, var(--accent), var(--sage), var(--lilac), var(--gold))',
};

export const headerBorderStyle: React.CSSProperties = {
    borderColor: 'var(--border-soft)',
};
